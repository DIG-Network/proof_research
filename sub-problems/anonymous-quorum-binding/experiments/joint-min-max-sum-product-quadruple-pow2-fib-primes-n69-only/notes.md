# Notes — joint-min-max-sum-product-quadruple-pow2-fib-primes-n69-only

- **observation:** `C(69,6)` is the next step after `n=68`; wall time ~365 s for all three schedules on this runner.
- **observation:** No witness for any schedule; same exhaustive pattern as **101–112**.
- **question:** Whether a collision appears for these schedules at some larger `n` remains open; next single-`n` step is **`n=70`** (again larger `C(n,6)`).
