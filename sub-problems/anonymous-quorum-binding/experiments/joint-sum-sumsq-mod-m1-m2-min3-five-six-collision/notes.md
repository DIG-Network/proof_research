# Notes

- **Same witnesses as 054** for the first hit: heavy tail 5-set vs initial block 6-set. The **054** obstruction at `(2,2)` was **bit-level redundancy**; here **`(3,3)`** is a genuine **mod-3** alignment on both **linear** and **quadratic** aggregates (not a duplicated coordinate).
- **Next knobs:** (i) require **`gcd(M₁, M₂) = 1`** and/or **`M₂` prime** and scan; (ii) **third** statistic (e.g. Σw³ mod M₃); (iii) larger **`n`** if we want “first collision” to move right without changing the toy shell geometry.
- **Binding thread:** These experiments continue to support that **fixed-degree symmetric modular summaries** on **static public weights** are **cheaply collidable** across the 5 vs 6 threshold interface for small **n**; they do **not** establish impossibility for all compact summaries.
