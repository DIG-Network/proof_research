# Notes

- **Lex-first coprime pair with both ≥4** is **(4,5)** — first collision appears **immediately** in that restricted family (no need to scan deep).
- **Recurring 6-shell** **(0..5)** still participates; the **5-shell** shifts when moduli exclude the **(4,6,7,8,9)** **vs** **(0..5)** alignment that drove **(2,3)** / **(3,3)**.
- **Next:** **min_m = 5** coprime scan; or **triple** **(Σw, Σw², Σw³)** with **pairwise coprime** moduli; or characterize **which** **6-set** classes stay universal “attractors” under these power sums.
