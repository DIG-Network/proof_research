# Notes

**Unsticking (persona — encoding change):** Treat validators as **evaluation values** of a **degree-(n−1)** interpolant instead of a flat Merkle list; ask what **one** extra field element buys. Answer in this toy: **not uniqueness** of the underlying **n**-tuple.

**Digest hook (persona “connections worth exploring”):** **Algebraic geometry codes / polynomial encoding** — transferred only as a **negative** lesson here: **single-point** data is **underdetermined** unless paired with a **binding** vector commitment and a **threshold-specific** predicate (still missing in the main problem).

**Not claimed:** Impossibility of **sublinear** honest schemes — only a **pedagogical** obstruction for a **too-weak** “**one eval**” verifier fantasy.
