# Notes

## Unsticking / encoding change

Follows **persona** “encoding change” after a long run of additive / modular / bitwise summaries: dualize **089** from **gcd (meet)** to **lcm (join)** on the same `w_i=i+1` universe.

## Comparison to 089

- **089:** All six-sets have `gcd=1`; joint `(gcd,Σw)` had **20** cross-shell keys.
- **Here:** `h_lcm` collapses even harder (**23** lcm values on six-sets, **all** appear on five-sets). Joint ambiguity is larger (**80** vs **20**), so join-side information is **not** sharper than meet-side for threshold separation on this instance.

## Next directions

- Avoid another scalar symmetric function of `{w_i}` unless tied to a new **binding** mechanism to `C` (the real bottleneck per digest).
- Consider **ordered** fingerprints (not permutation-invariant) — breaks anonymity unless randomized per epoch with commitment.
- Revisit **verifier-oracle-model** resource formalization vs **anonymous-quorum-binding** when a non-toy primitive candidate appears.
