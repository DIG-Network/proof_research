# Notes

- **Binding** could try to fold `k` into `σ` (e.g. sign `(m, k)`) — then **`k`** is tied to **that** signature under **`K`**, but **`K`** still needs **`Link(C, K)`** to exclude foreign / undersized coalitions (**002**).
- This experiment is **orthogonal** to **008** (rogue keys): here **`k̂`** is simply **forgeable metadata**.
