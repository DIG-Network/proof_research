# Notes

- The collision at `M=2` is driven by parity on all four power sums for these subset sizes (same mechanism as the first-moment-only scan when the first hit was `M=2`).
- For anonymous binding via modular moments, four uniform moments do not separate 5-subsets from 6-subsets at the smallest modulus where any separation could occur; need either larger `n`/different weights, non-uniform or structured moments, or a different encoding than raw power sums mod `M`.
- Next: try **five** power sums `k=1..5` for the same scan, or weights not in arithmetic progression, to see if the first collision modulus moves above 2.
