# Notes — joint-min-max-sum-product-quadruple-pow2-fib-primes-n90-only

- **Design:** Same exhaustive map+scan as **101–133**; only `n` advances to **90**.
- **Compute:** `C(90,6)` six-subset scans per schedule; expect wall time modestly above **133** (`n=89`).
- **If FAIL:** Combined with **101–133**, extends the negative band to **`n ∈ [11,90]`** for these three schedules under exact **`K`**.
