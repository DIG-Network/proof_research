# Notes

- **Encoding change:** Same **min/max** anchors as **091**; **product** is the **meet**-side multiplicative analogue to **sum** (dual spirit to **090** lcm / **089** gcd on the same weights).
- **Exact collisions:** **31** vs **41** for **(min,max,sum)** — **product** is **more discriminating** on this finite universe but **still** far from injective on \(C(10,5)\cup C(10,6)\).
- **Mod 2:** For **odd** weights \(1..11\) on indices, **\(\prod S\)** is odd iff **\(|S|\)** is odd — so **5-shell** and **6-shell** **both** hit **even** and **odd** products; joint **(min,max,prod mod 2)** collapses like **(min,max,sum mod 2)** once **min/max** allow both parities (here **first \(M\)** is still **2**).
- **Next:** If pursuing **joint** tags, **(min,max,sum,prod)** exact might still collide (063 style: sum+product had **37** cross-shell); or move to **verifier-oracle-model** per digest.
