# Notes

- **Runtime:** Default **`--scan-max 18`** finished in ~6s; raising cap only matters if **INCONCLUSIVE** at default.
- **Takeaway:** **Per-moment coprime moduli** still **do not** give a robust **compact** separator: first collision uses **very small** **M_i** **(max 13)**. The **gap** between **(2,3,5,7,11)** **and** **(2,3,5,7,13)** shows **p₅ mod M₅** **can** **matter** **for** **lex** **order** **—** **unlike** **uniform** **single** **M** **where** **w^k≡w (mod 2)** **collapsed** **everything** **at** **M=2**.
- **Next:** Optional **`--min-m 4`** variant (parallel **060**) to see lex-first **5-tuple** of **minima**; or leave moment line and return **verifier-oracle-model**.
