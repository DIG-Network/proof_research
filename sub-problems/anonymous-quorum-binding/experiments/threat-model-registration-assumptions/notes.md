# Notes

- **Main problem** `problem-statement.md` was patched in the same iteration to make **registration** explicit; align security games when formalizing **soundness**.
- If the product goal is **fully malicious** registration with **no** PoP and **no** per-key material at verify time, Entry **008** suggests **aggregate-only** interfaces remain **dangerous** even if **`Link`** were efficient — collisions could map to ambiguous coalitions in richer group models (separate analysis).
