# Notes — joint-min-max-sum-product-quadruple-pow2-fib-primes-n66-only

- **observation:** `C(66,6)` is the next large jump after `n=65`; wall time ~277 s for all three schedules on this runner.
- **observation:** No witness for any schedule; same exhaustive pattern as **101–109**.
- **question:** Whether a collision appears for these schedules at some larger `n` remains open; next single-`n` step is **`n=67`** (again larger `C(n,6)`).
