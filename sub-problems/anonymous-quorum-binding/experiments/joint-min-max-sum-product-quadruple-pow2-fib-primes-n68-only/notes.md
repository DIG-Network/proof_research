# Notes — joint-min-max-sum-product-quadruple-pow2-fib-primes-n68-only

- **observation:** `C(68,6)` is the next step after `n=67`; wall time ~341 s for all three schedules on this runner.
- **observation:** No witness for any schedule; same exhaustive pattern as **101–111**.
- **question:** Whether a collision appears for these schedules at some larger `n` remains open; next single-`n` step is **`n=69`** (again larger `C(n,6)`).
