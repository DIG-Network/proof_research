# Notes — joint-min-max-sum-product-quadruple-pow2-fib-primes-n70-only

- **observation:** `C(70,6)` is the next step after `n=69`; wall time ~394 s for all three schedules on this runner.
- **observation:** No witness for any schedule; same exhaustive pattern as **101–113**.
- **question:** Whether a collision appears for these schedules at some larger `n` remains open; next single-`n` step is **`n=71`** (again larger `C(n,6)`).
