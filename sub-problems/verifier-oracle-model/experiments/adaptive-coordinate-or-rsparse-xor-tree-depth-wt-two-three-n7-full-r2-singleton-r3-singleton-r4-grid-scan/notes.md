# Notes

- **Complement law:** The 35 depth-2 cells are exactly `(T, [7]\setminus T)` in index space — not “triple contained in quad” (that would be 140 pairs).
- **Contrast:** Full `r=3` menu (35 splits) + any one `r=4` → all 35 choices gave `min_d=2` (experiment `…-n7-full-r2-r3-plus-each-r4-split-once`). Here, sparsifying triples to **one** split breaks that unless the quad is complementary.
- **Next:** If pursuing minimal certificates: characterize **minimal** `(r3 multiset, r4 multiset)` for `min_d=2` at `n=7`; or move to anonymous-quorum-binding per `session-state.md` alternation.
