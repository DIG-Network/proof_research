# Notes

## observation

`W_ij` and `W_ji` are distinct on this stratum (`pred_both=0`); the `770` depth-2 witnesses split evenly (`385` each orientation).

## insight

Ordered wedge alone (exp 161) was a **one-way** sound predicate; taking **`W_ij ∨ W_ji`** is the minimal symmetric repair and yields a **full biconditional** with `min_d=2` here.

## analogy

Union of two orientations matches “symmetric closure” of a directed certificate — same structural move as making an undirected edge from two directed half-edges.

## question

Does a similar **orientation-union** patch combine with the `|∩|=2` patchwork (`W` vs complement-symdiff) into a single global classifier for all off-diagonal `n=7` `{2,3}` doubleton-triple menus?

## next

Stratify by `s=|∩|` and test a unified template across `s∈{0,1,2}` if desired; or document this stratum law and move to `anonymous-quorum-binding` per session plan.
