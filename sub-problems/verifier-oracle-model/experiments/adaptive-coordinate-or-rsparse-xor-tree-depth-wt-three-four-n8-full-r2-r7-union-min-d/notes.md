# Notes

- **observation:** `total_splits=246` = Σ_{r=2}^{7} C(8,r) = 28+56+70+56+28+8.
- **insight:** Baselines mirror larger-`n` pattern: coordinate-only needs full depth; full parity collapses to `min_d=1`; union restores `min_d=2`.
- **question:** Whether `n=7` `{2,3}` admits the same full-menu `min_d=2` (smaller domain; optional ladder continuation).
