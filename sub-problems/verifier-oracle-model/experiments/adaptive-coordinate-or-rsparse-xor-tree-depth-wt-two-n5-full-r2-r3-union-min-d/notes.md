# Notes

## observation

At **`n=5`**, **`coord_only`** reaches **`min_d=1`** immediately (**`d_max=5`**), unlike **`n≥6`** shells where **`coord_only`** needs depth **`n`**.

## dead_end

Treating **`n=5`** **`{2}`** as part of the same **`min_d=2`** narrative as **`n∈{6,…,14}`** is misleading: the union menu is **not** depth-**`2`**-minimal here (**`min_d=1`**).

## insight

The **`min_d=2`** full-union phenomenon is a **finite-size** / **mask-count** effect that **turns on** at **`n=6`** for the majority-adjacent **`{2,3}`** pattern (and **`n≥6`** more generally in this ladder); **`n=5`** is below that threshold for this shell.

## question

Whether any **different** shell or auxiliary split menu at **`n=5`** recovers a nontrivial **`min_d>1`** certificate is orthogonal to this wrapper’s **`{2}`**-only slice.
