# Notes

- **R2** nuance: revealing **only** `t` indices might be `O(t log n)` bits — still often **linear in `n`** when `t = Θ(n)`; partial revelation might preserve **some** anonymity — not explored numerically here.
- **Hybrid relaxations** (e.g. sublinear `π` + **small** `n` only) are out of scope for asymptotic main statement.
- If the project **ABANDONs** the strict main goal, use this ladder to name **which R_i** the **revised** problem adopts.
