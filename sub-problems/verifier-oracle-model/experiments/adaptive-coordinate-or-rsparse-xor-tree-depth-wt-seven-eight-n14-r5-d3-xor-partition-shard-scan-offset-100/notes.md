# Notes

- Mirrors `…-shard-scan-400` wrapper; only `SHARDS` list changes (offset 100).
- Indices `0..99` of the full `r=5` XOR menu are **not** included in this run.
- Interpretation matches prior shard entry: negative shards do not prove full-menu `d=3` infeasible.
