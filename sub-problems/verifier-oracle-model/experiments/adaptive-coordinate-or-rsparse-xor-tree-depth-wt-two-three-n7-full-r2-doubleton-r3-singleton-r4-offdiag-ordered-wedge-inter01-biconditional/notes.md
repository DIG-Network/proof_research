# Notes

- **Ordering helps soundness vs symdiff:** On this stratum, `Q=W(i,j)` never occurs with `min_d≠2` (contrast symdiff, which had **315** `Q=symdiff` ∧ `min_d=3` cells).
- **Still not a characterization:** **385** `min_d=2` witnesses have `Q≠W`; the symdiff experiment had **630** `min_d=2` ∧ `Q≠symdiff` — different splits, but both show **no single 4-set template** captures all depth-2 quartics here.
- **Next:** Try **two-chart** disjunctions on `s∈{0,1}` (e.g. `W(i,j)` ∪ `W(j,i)` ∪ symdiff ∪ complement templates), **small DNF** over bitmask atoms, or pivot to **anonymous-quorum-binding** per session plan.
