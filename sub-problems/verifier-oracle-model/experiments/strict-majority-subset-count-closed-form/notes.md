# Notes

- The odd-\(n\) identity \(N(n)=2^{n-1}\) is the standard symmetry \(\binom{n}{k}=\binom{n}{n-k}\) on a chain without a middle row.
- Even-\(n\) subtracts the central binomial \(\binom{n}{n/2}\) before halving — the script’s brute range catches off-by-one mistakes immediately.
- **Verifier-oracle hook:** any ideal functionality that treats “which strict-majority coalition” as a **uniform** hidden variable carries **\(\Theta(n)\)** raw index entropy in this counting sense; **006** still blocks mapping that to \(|\pi|\) for **anonymous** realizations.
