# Notes

- **Analogy thread:** The construction is the standard “**parity checks orthogonal to a fixed difference**” trick from **coding theory**; the experiment makes that isomorphism explicit for the **threshold** narrative.
- **Limitation:** Cryptographic verifiers are **not** bare **F₂** linear functionals of participation bits; this is a **pedagogical lower bound on a straw model**, not an impossibility for the full problem.
- **Unsticking hook (persona):** If stuck on “**compress validator state then probe**,” this suggests **encoding change** or **nonlinear** certification — pure **linear pooling** is structurally ambiguous.
