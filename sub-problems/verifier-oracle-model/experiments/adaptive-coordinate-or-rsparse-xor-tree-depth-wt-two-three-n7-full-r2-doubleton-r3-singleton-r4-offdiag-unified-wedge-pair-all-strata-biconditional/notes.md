# Notes

- **observation:** Unifies experiments 162 (`s∈{0,1}`) and 164 (`s=2` with `W∨W_rev∨C` ≡ `W∨W_rev`) into one global off-diagonal biconditional.
- **insight:** The stratified rule in journal 163 failed only because the `s=2` branch omitted `W_ji`; adding both orientations everywhere removes the need for case-splitting by `s` on this grid.
- **question:** Does this pattern persist for other `n` or other XOR-menu shapes beyond this fixed `n=7` doubleton-triple + singleton-quartic language?
