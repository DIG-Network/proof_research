# Notes

## observation

At n=8 with shell masks wt 2∪3, the language **coord + full r=2 XOR + doubleton r=3 + singleton r=4** never yields `min_d=2` on any off-diagonal cell with `|T_i∩T_j|≤2` in the full scan; all sampled progress lines showed `md=3`.

## dead_end

Treating this PASS as evidence that “n=8 behaves like n=7” would be wrong: n=7 had a **non-empty** depth-2 stratum tightly matched by wedges. Here the predicate is **uninformative** (empty positive class).

## insight

Vacuous biconditionals are PASS for the script’s violation logic but carry **different scientific weight** than n=7’s structural certificate. Any verifier-facing law at n=8 needs a **different depth** or richer split menu before wedge certificates become meaningful.

## question

Does there exist an n=8 extension of the menu (e.g. additional XOR arity, or diagonal `i=j` cells) where `min_d=2` reappears and wedge geometry again characterizes it?
