# Notes — wedge ∪ complement(symdiff) on `s=2`

- **Half of stratum depth-2 cells** escape the **two-chart** union: **`420`** **`min_d=2`** vs **`210`** **`Q`** hits **`{W,C}`** — a clean **2×** gap on **`|∩|=2`** alone.
- **Fixed quartic index** **`k=34`** appears in many violations for **`T_i=(0,1,2)`**-type rows — suggests **`Q`** (menu index) couples to **global** shell geometry, not only **`(T_i,T_j)`** set algebra.
- **Complement(symdiff)** for **`|T_i△T_j|=2`** is a **5-set**, so it **never** equals a **4-set** **`Q`** in this experiment; the **`210`** predicate hits are **entirely** the **wedge** branch (the **`C`** branch is **vacuous** on quartics here). The **FAIL** is driven by **`min_d=2`** witnesses **outside** **`W`** (and outside **`C`** trivially).
- **Next:** abandon **pure** **`(T_i,T_j)`**-only formulas; try **(i)** include **`k`** / quartic **mask** in a **small DNF** search over richer atoms, **(ii)** **`|∩|∈{0,1}`** closed predicates from session-state, or **(iii)** pivot to **anonymous-quorum-binding** digest.
