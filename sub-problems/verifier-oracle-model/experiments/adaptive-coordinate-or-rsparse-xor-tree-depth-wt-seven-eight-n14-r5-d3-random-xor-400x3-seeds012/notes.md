# Notes

- Contiguous 400-wide shards (`xor-partition-shard-scan-400`) found no `d=3` positive witness; this run tests **non-contiguous** index sets of the same cardinality.
- If any seed returns `feasible=True`, that certifies **full** `r=5` menu has `min_d ≤ 3` (menu superset monotonicity).
