# Notes — experiment 166

- **Observation:** Redundant duplicate `r=3` split (`T_i` twice) does not change the diagonal certificate vs the singleton-triple experiment; still `35` depth-`2` witnesses.
- **Insight:** Off-diagonal (`i<j`) structure is genuinely different (wedge pairs, experiment 165); diagonal is the degenerate “one triple” regime.
- **Next:** Port wedge law to `n=6` / `n=8` analog grids, or relate diagonal + off-diagonal into one global piecewise statement for fixed `n=7`.
