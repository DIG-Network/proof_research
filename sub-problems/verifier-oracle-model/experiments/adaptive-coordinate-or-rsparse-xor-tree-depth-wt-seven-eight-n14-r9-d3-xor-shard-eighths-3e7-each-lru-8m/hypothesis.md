# Hypothesis: eighth-shard `r=9` XOR menu, `d=3`

## Analogy pass

1. **Abstract structure:** Same as the `r=5` eighth-shard probe: test whether **finer** contiguous XOR blocking at **fixed** aggregate **2.4×10⁸** `exists_tree` changes PARTIAL vs complete **`d=3`** verdicts relative to **quarter** geometry.

2. **Analogous domains:** (a) **Dual** codes — `r` and `n−r` share split count **C(14,9)=C(14,5)=2002** but DP behavior need not match; still, **parallel** instrumentation aids parity diagnosis. (b)–(c) Same as `r=5` eighth note (refinement / segmentation tradeoffs).

3. **Machinery:** Matched partitions and budgets across **dual** **2002** rows.

4. **Transfer candidate:** Mirror **`r=5`** eighth-shard wrapper with **`--r-single 9`** — eight ranges, **3×10⁷**/**8M** each — and compare wall times and exit codes to **`r=5`** and to **`r=9`** **quarters**.

## Falsifiable claim

**`r=9`** eighths either **all** complete with **`d=3 feasible=False`** (like quarters) **or** exhibit **PARTIAL** / a witness, refuting the “eighths behave like quarters” parallelism at this envelope.
