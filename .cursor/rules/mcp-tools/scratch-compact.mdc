---
description: "MCP tool scratch_compact (Cursor server \"scratch\", HTTP mount /mcp/scratch). Args include required project id. When: Archive many scratch ids into one canonical body under a new key (cleanup before handoff). Slugs: scratch-compact, scratch_compact, mcp-scratch-compact, tool-scratch-compact, scratch/scratch-compact, compact, scratch/compact. Relevance phrases: User or agent explicitly names `scratch_compact` or equivalent (scratch compact) · Workflow step that matches this capability: scratch compact · Archive many scratch ids into one canonical body under a new key (cleanup before handoff). · Short-lived notes, hypotheses, snippets, draft bullets during an agent turn · TTL, importance, pin/unpin, compaction into one canonical scratch key. Keyword index: scratch_compact scratch /mcp/scratch scratch-compact scratch_compact mcp-scratch-compact tool-scratch-compact scratch/scratch-compact compact scratch/compact user or agent explicitly names `scratch_compact` or equivalent (scratch compact) workflow step that matches this capability: scratch compact archive many scratch ids into one canonical body under a new key (cleanup before handoff). short-lived notes, hypotheses, snippets, draft bullets during an agent turn ttl, importance, pin/unpin, compaction into one canonical scratch key archive many scratch ids into one canonical body under a new key (cleanup before handoff)."
alwaysApply: false
---

# `scratch_compact`

**Cursor MCP server:** `scratch`  
**HTTP mount:** `/mcp/scratch`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `scratch-compact`
- `scratch_compact`
- `mcp-scratch-compact`
- `tool-scratch-compact`
- `scratch/scratch-compact`
- `compact`
- `scratch/compact`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `scratch_compact` or equivalent (scratch compact)
- Workflow step that matches this capability: scratch compact
- Archive many scratch ids into one canonical body under a new key (cleanup before handoff).
- Short-lived notes, hypotheses, snippets, draft bullets during an agent turn
- TTL, importance, pin/unpin, compaction into one canonical scratch key

### Summary

Archive many scratch ids into one canonical body under a new key (cleanup before handoff).

## How to use

- Use when instructed by handoff/session workflows; see tool schema for `scratch_ids`, `new_key`, `canonical_body`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
