---
description: "MCP tool planner_task_link (Cursor server \"planner\", HTTP mount /mcp/planner). Args include required project id. When: Task B must finish before task A can be treated as ready (dependency edge). Slugs: planner-task-link, planner_task_link, mcp-planner-task-link, tool-planner-task-link, planner/planner-task-link, task-link, planner/task-link. Relevance phrases: User or agent explicitly names `planner_task_link` or equivalent (planner task link) · Workflow step that matches this capability: planner task link · Task B must finish before task A can be treated as ready (dependency edge). · Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans · Export/import task graph JSON for backup or migration · Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder. Keyword index: planner_task_link planner /mcp/planner planner-task-link planner_task_link mcp-planner-task-link tool-planner-task-link planner/planner-task-link task-link planner/task-link user or agent explicitly names `planner_task_link` or equivalent (planner task link) workflow step that matches this capability: planner task link task b must finish before task a can be treated as ready (dependency edge). task dag: dependencies, ready work, blocking relationships, multi-step implementation plans export/import task graph json for backup or migration task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder task b must finish before task a can be treated as ready (dependency edge)."
alwaysApply: false
---

# `planner_task_link`

**Cursor MCP server:** `planner`  
**HTTP mount:** `/mcp/planner`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `planner-task-link`
- `planner_task_link`
- `mcp-planner-task-link`
- `tool-planner-task-link`
- `planner/planner-task-link`
- `task-link`
- `planner/task-link`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `planner_task_link` or equivalent (planner task link)
- Workflow step that matches this capability: planner task link
- Task B must finish before task A can be treated as ready (dependency edge).
- Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans
- Export/import task graph JSON for backup or migration
- Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder

### Summary

Task B must finish before task A can be treated as ready (dependency edge).

## How to use

- Required: `project`, `task_id`, `depends_on_task_id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
