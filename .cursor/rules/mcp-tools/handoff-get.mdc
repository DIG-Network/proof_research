---
description: "MCP tool handoff_get (Cursor server \"handoff\", HTTP mount /mcp/handoff). Args include required project id. When: Fetch full body for one saved handoff id. Slugs: handoff-get, handoff_get, mcp-handoff-get, tool-handoff-get, handoff/handoff-get. Relevance phrases: User or agent explicitly names `handoff_get` or equivalent (handoff get) · Workflow step that matches this capability: handoff get · Fetch full body for one saved handoff id. · Assemble markdown handoff pack from planner + ledger + scratch + artifacts + constraints · Validate workspace counts; save/list/get versioned handoff snapshots for session restore. Keyword index: handoff_get handoff /mcp/handoff handoff-get handoff_get mcp-handoff-get tool-handoff-get handoff/handoff-get user or agent explicitly names `handoff_get` or equivalent (handoff get) workflow step that matches this capability: handoff get fetch full body for one saved handoff id. assemble markdown handoff pack from planner + ledger + scratch + artifacts + constraints validate workspace counts; save/list/get versioned handoff snapshots for session restore fetch full body for one saved handoff id."
alwaysApply: false
---

# `handoff_get`

**Cursor MCP server:** `handoff`  
**HTTP mount:** `/mcp/handoff`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `handoff-get`
- `handoff_get`
- `mcp-handoff-get`
- `tool-handoff-get`
- `handoff/handoff-get`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `handoff_get` or equivalent (handoff get)
- Workflow step that matches this capability: handoff get
- Fetch full body for one saved handoff id.
- Assemble markdown handoff pack from planner + ledger + scratch + artifacts + constraints
- Validate workspace counts; save/list/get versioned handoff snapshots for session restore

### Summary

Fetch full body for one saved handoff id.

## How to use

- Required: `project`, `id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
