---
description: "MCP tool spec_link_task (Cursor server \"spec\", HTTP mount /mcp/spec). Args include required project id. When: Associate a spec with a planner task. Slugs: spec-link-task, spec_link_task, mcp-spec-link-task, tool-spec-link-task, spec/spec-link-task, link-task, spec/link-task. Relevance phrases: User or agent explicitly names `spec_link_task` or equivalent (spec link task) · Workflow step that matches this capability: spec link task · Associate a spec with a planner task. · Requirements / acceptance criteria: define, link to planner tasks, set satisfaction status · Spec rollup summary for dashboards or cron hints. Keyword index: spec_link_task spec /mcp/spec spec-link-task spec_link_task mcp-spec-link-task tool-spec-link-task spec/spec-link-task link-task spec/link-task user or agent explicitly names `spec_link_task` or equivalent (spec link task) workflow step that matches this capability: spec link task associate a spec with a planner task. requirements / acceptance criteria: define, link to planner tasks, set satisfaction status spec rollup summary for dashboards or cron hints associate a spec with a planner task."
alwaysApply: false
---

# `spec_link_task`

**Cursor MCP server:** `spec`  
**HTTP mount:** `/mcp/spec`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `spec-link-task`
- `spec_link_task`
- `mcp-spec-link-task`
- `tool-spec-link-task`
- `spec/spec-link-task`
- `link-task`
- `spec/link-task`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `spec_link_task` or equivalent (spec link task)
- Workflow step that matches this capability: spec link task
- Associate a spec with a planner task.
- Requirements / acceptance criteria: define, link to planner tasks, set satisfaction status
- Spec rollup summary for dashboards or cron hints

### Summary

Associate a spec with a planner task.

## How to use

- Required: `project`, `spec_id`, `task_id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
