---
description: "MCP tool planner_import_json (Cursor server \"planner\", HTTP mount /mcp/planner). Args include required project id. When: Restore or merge a previously exported task graph for this project. Slugs: planner-import-json, planner_import_json, mcp-planner-import-json, tool-planner-import-json, planner/planner-import-json, import-json, planner/import-json. Relevance phrases: User or agent explicitly names `planner_import_json` or equivalent (planner import json) · Workflow step that matches this capability: planner import json · Restore or merge a previously exported task graph for this project. · Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans · Export/import task graph JSON for backup or migration · Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder. Keyword index: planner_import_json planner /mcp/planner planner-import-json planner_import_json mcp-planner-import-json tool-planner-import-json planner/planner-import-json import-json planner/import-json user or agent explicitly names `planner_import_json` or equivalent (planner import json) workflow step that matches this capability: planner import json restore or merge a previously exported task graph for this project. task dag: dependencies, ready work, blocking relationships, multi-step implementation plans export/import task graph json for backup or migration task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder restore or merge a previously exported task graph for this project."
alwaysApply: false
---

# `planner_import_json`

**Cursor MCP server:** `planner`  
**HTTP mount:** `/mcp/planner`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `planner-import-json`
- `planner_import_json`
- `mcp-planner-import-json`
- `tool-planner-import-json`
- `planner/planner-import-json`
- `import-json`
- `planner/import-json`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `planner_import_json` or equivalent (planner import json)
- Workflow step that matches this capability: planner import json
- Restore or merge a previously exported task graph for this project.
- Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans
- Export/import task graph JSON for backup or migration
- Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder

### Summary

Restore or merge a previously exported task graph for this project.

## How to use

- Required: `project` plus export payload per tool schema.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
