---
description: "MCP tool release_list (Cursor server \"release\", HTTP mount /mcp/release). Args include required project id. When: List recent release jobs. Slugs: release-list, release_list, mcp-release-list, tool-release-list, release/release-list. Relevance phrases: User or agent explicitly names `release_list` or equivalent (release list) · Workflow step that matches this capability: release list · List recent release jobs. · Release automation jobs: trigger, list, get status (host may disable or allowlist webhooks). Keyword index: release_list release /mcp/release release-list release_list mcp-release-list tool-release-list release/release-list user or agent explicitly names `release_list` or equivalent (release list) workflow step that matches this capability: release list list recent release jobs. release automation jobs: trigger, list, get status (host may disable or allowlist webhooks) list recent release jobs."
alwaysApply: false
---

# `release_list`

**Cursor MCP server:** `release`  
**HTTP mount:** `/mcp/release`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `release-list`
- `release_list`
- `mcp-release-list`
- `tool-release-list`
- `release/release-list`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `release_list` or equivalent (release list)
- Workflow step that matches this capability: release list
- List recent release jobs.
- Release automation jobs: trigger, list, get status (host may disable or allowlist webhooks)

### Summary

List recent release jobs.

## How to use

- Required: `project`. Optional `limit`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
