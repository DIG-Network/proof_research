---
description: "MCP tool verify_compare (Cursor server \"verify\", HTTP mount /mcp/verify). Args include required project id. When: Diff stdout_text between two runs. Slugs: verify-compare, verify_compare, mcp-verify-compare, tool-verify-compare, verify/verify-compare, compare, verify/compare. Relevance phrases: User or agent explicitly names `verify_compare` or equivalent (verify compare) · Workflow step that matches this capability: verify compare · Diff stdout_text between two runs. · Run recorded HTTP (or shell if enabled) checks; tie to specs; compare run outputs. Keyword index: verify_compare verify /mcp/verify verify-compare verify_compare mcp-verify-compare tool-verify-compare verify/verify-compare compare verify/compare user or agent explicitly names `verify_compare` or equivalent (verify compare) workflow step that matches this capability: verify compare diff stdout_text between two runs. run recorded http (or shell if enabled) checks; tie to specs; compare run outputs diff stdout_text between two runs."
alwaysApply: false
---

# `verify_compare`

**Cursor MCP server:** `verify`  
**HTTP mount:** `/mcp/verify`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `verify-compare`
- `verify_compare`
- `mcp-verify-compare`
- `tool-verify-compare`
- `verify/verify-compare`
- `compare`
- `verify/compare`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `verify_compare` or equivalent (verify compare)
- Workflow step that matches this capability: verify compare
- Diff stdout_text between two runs.
- Run recorded HTTP (or shell if enabled) checks; tie to specs; compare run outputs

### Summary

Diff stdout_text between two runs.

## How to use

- Required: `project`, `run_id_a`, `run_id_b`. Optional `max_chars`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
