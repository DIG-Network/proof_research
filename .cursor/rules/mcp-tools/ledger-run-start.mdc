---
description: "MCP tool ledger_run_start (Cursor server \"ledger\", HTTP mount /mcp/ledger). Args include required project id. When: Begin an episodic run (feature, cron tick, deploy) you will append events to. Slugs: ledger-run-start, ledger_run_start, mcp-ledger-run-start, tool-ledger-run-start, ledger/ledger-run-start, run-start, ledger/run-start. Relevance phrases: User or agent explicitly names `ledger_run_start` or equivalent (ledger run start) · Workflow step that matches this capability: ledger run start · Begin an episodic run (feature, cron tick, deploy) you will append events to. · Episodic run log: start run, append events, search history, close run · Audit trail for cron ticks, deploys, long agent loops · Checkpoint + optional close in one ledger call. Keyword index: ledger_run_start ledger /mcp/ledger ledger-run-start ledger_run_start mcp-ledger-run-start tool-ledger-run-start ledger/ledger-run-start run-start ledger/run-start user or agent explicitly names `ledger_run_start` or equivalent (ledger run start) workflow step that matches this capability: ledger run start begin an episodic run (feature, cron tick, deploy) you will append events to. episodic run log: start run, append events, search history, close run audit trail for cron ticks, deploys, long agent loops checkpoint + optional close in one ledger call begin an episodic run (feature, cron tick, deploy) you will append events to."
alwaysApply: false
---

# `ledger_run_start`

**Cursor MCP server:** `ledger`  
**HTTP mount:** `/mcp/ledger`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `ledger-run-start`
- `ledger_run_start`
- `mcp-ledger-run-start`
- `tool-ledger-run-start`
- `ledger/ledger-run-start`
- `run-start`
- `ledger/run-start`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `ledger_run_start` or equivalent (ledger run start)
- Workflow step that matches this capability: ledger run start
- Begin an episodic run (feature, cron tick, deploy) you will append events to.
- Episodic run log: start run, append events, search history, close run
- Audit trail for cron ticks, deploys, long agent loops
- Checkpoint + optional close in one ledger call

### Summary

Begin an episodic run (feature, cron tick, deploy) you will append events to.

## How to use

- Required: `project`. Optional `title`, `metadata`. Returns `run_id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
