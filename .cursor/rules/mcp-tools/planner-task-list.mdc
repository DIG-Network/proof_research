---
description: "MCP tool planner_task_list (Cursor server \"planner\", HTTP mount /mcp/planner). Args include required project id. When: List/filter tasks (status, parent, limit). Slugs: planner-task-list, planner_task_list, mcp-planner-task-list, tool-planner-task-list, planner/planner-task-list, task-list, planner/task-list. Relevance phrases: User or agent explicitly names `planner_task_list` or equivalent (planner task list) · Workflow step that matches this capability: planner task list · List/filter tasks (status, parent, limit). · Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans · Export/import task graph JSON for backup or migration · Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder. Keyword index: planner_task_list planner /mcp/planner planner-task-list planner_task_list mcp-planner-task-list tool-planner-task-list planner/planner-task-list task-list planner/task-list user or agent explicitly names `planner_task_list` or equivalent (planner task list) workflow step that matches this capability: planner task list list/filter tasks (status, parent, limit). task dag: dependencies, ready work, blocking relationships, multi-step implementation plans export/import task graph json for backup or migration task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder list/filter tasks (status, parent, limit)."
alwaysApply: false
---

# `planner_task_list`

**Cursor MCP server:** `planner`  
**HTTP mount:** `/mcp/planner`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `planner-task-list`
- `planner_task_list`
- `mcp-planner-task-list`
- `tool-planner-task-list`
- `planner/planner-task-list`
- `task-list`
- `planner/task-list`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `planner_task_list` or equivalent (planner task list)
- Workflow step that matches this capability: planner task list
- List/filter tasks (status, parent, limit).
- Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans
- Export/import task graph JSON for backup or migration
- Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder

### Summary

List/filter tasks (status, parent, limit).

## How to use

- Required: `project`. Optional `status`, `parent_task_id`, `limit`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
