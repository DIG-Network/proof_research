---
description: "MCP tool handoff_list (Cursor server \"handoff\", HTTP mount /mcp/handoff). Args include required project id. When: List saved handoff snapshots newest first. Slugs: handoff-list, handoff_list, mcp-handoff-list, tool-handoff-list, handoff/handoff-list. Relevance phrases: User or agent explicitly names `handoff_list` or equivalent (handoff list) · Workflow step that matches this capability: handoff list · List saved handoff snapshots newest first. · Assemble markdown handoff pack from planner + ledger + scratch + artifacts + constraints · Validate workspace counts; save/list/get versioned handoff snapshots for session restore. Keyword index: handoff_list handoff /mcp/handoff handoff-list handoff_list mcp-handoff-list tool-handoff-list handoff/handoff-list user or agent explicitly names `handoff_list` or equivalent (handoff list) workflow step that matches this capability: handoff list list saved handoff snapshots newest first. assemble markdown handoff pack from planner + ledger + scratch + artifacts + constraints validate workspace counts; save/list/get versioned handoff snapshots for session restore list saved handoff snapshots newest first."
alwaysApply: false
---

# `handoff_list`

**Cursor MCP server:** `handoff`  
**HTTP mount:** `/mcp/handoff`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `handoff-list`
- `handoff_list`
- `mcp-handoff-list`
- `tool-handoff-list`
- `handoff/handoff-list`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `handoff_list` or equivalent (handoff list)
- Workflow step that matches this capability: handoff list
- List saved handoff snapshots newest first.
- Assemble markdown handoff pack from planner + ledger + scratch + artifacts + constraints
- Validate workspace counts; save/list/get versioned handoff snapshots for session restore

### Summary

List saved handoff snapshots newest first.

## How to use

- Required: `project`. Optional `limit`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
