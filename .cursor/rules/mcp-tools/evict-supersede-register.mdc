---
description: "MCP tool evict_supersede_register (Cursor server \"evict\", HTTP mount /mcp/evict). Args include required project id. When: Mark older scratch rows superseded by a newer id for GC heuristics. Slugs: evict-supersede-register, evict_supersede_register, mcp-evict-supersede-register, tool-evict-supersede-register, evict/evict-supersede-register, supersede-register, evict/supersede-register. Relevance phrases: User or agent explicitly names `evict_supersede_register` or equivalent (evict supersede register) · Workflow step that matches this capability: evict supersede register · Mark older scratch rows superseded by a newer id for GC heuristics. · GC hygiene: suggest stale scratch, archive to cold storage, supersede old rows. Keyword index: evict_supersede_register evict /mcp/evict evict-supersede-register evict_supersede_register mcp-evict-supersede-register tool-evict-supersede-register evict/evict-supersede-register supersede-register evict/supersede-register user or agent explicitly names `evict_supersede_register` or equivalent (evict supersede register) workflow step that matches this capability: evict supersede register mark older scratch rows superseded by a newer id for gc heuristics. gc hygiene: suggest stale scratch, archive to cold storage, supersede old rows mark older scratch rows superseded by a newer id for gc heuristics."
alwaysApply: false
---

# `evict_supersede_register`

**Cursor MCP server:** `evict`  
**HTTP mount:** `/mcp/evict`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `evict-supersede-register`
- `evict_supersede_register`
- `mcp-evict-supersede-register`
- `tool-evict-supersede-register`
- `evict/evict-supersede-register`
- `supersede-register`
- `evict/supersede-register`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `evict_supersede_register` or equivalent (evict supersede register)
- Workflow step that matches this capability: evict supersede register
- Mark older scratch rows superseded by a newer id for GC heuristics.
- GC hygiene: suggest stale scratch, archive to cold storage, supersede old rows

### Summary

Mark older scratch rows superseded by a newer id for GC heuristics.

## How to use

- See tool schema for ids and metadata.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
