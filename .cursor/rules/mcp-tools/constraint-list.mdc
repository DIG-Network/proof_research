---
description: "MCP tool constraint_list (Cursor server \"constraint\", HTTP mount /mcp/constraint). Args include required project id. When: List active constraints for the project. Slugs: constraint-list, constraint_list, mcp-constraint-list, tool-constraint-list, constraint/constraint-list. Relevance phrases: User or agent explicitly names `constraint_list` or equivalent (constraint list) · Workflow step that matches this capability: constraint list · List active constraints for the project. · Record invariants the code must satisfy; list or revoke constraints for the project. Keyword index: constraint_list constraint /mcp/constraint constraint-list constraint_list mcp-constraint-list tool-constraint-list constraint/constraint-list user or agent explicitly names `constraint_list` or equivalent (constraint list) workflow step that matches this capability: constraint list list active constraints for the project. record invariants the code must satisfy; list or revoke constraints for the project list active constraints for the project."
alwaysApply: false
---

# `constraint_list`

**Cursor MCP server:** `constraint`  
**HTTP mount:** `/mcp/constraint`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `constraint-list`
- `constraint_list`
- `mcp-constraint-list`
- `tool-constraint-list`
- `constraint/constraint-list`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `constraint_list` or equivalent (constraint list)
- Workflow step that matches this capability: constraint list
- List active constraints for the project.
- Record invariants the code must satisfy; list or revoke constraints for the project

### Summary

List active constraints for the project.

## How to use

- Required: `project`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
