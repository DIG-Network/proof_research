---
description: "MCP tool planner_task_get (Cursor server \"planner\", HTTP mount /mcp/planner). Args include required project id. When: Fetch one task row and its outgoing dependency ids. Slugs: planner-task-get, planner_task_get, mcp-planner-task-get, tool-planner-task-get, planner/planner-task-get, task-get, planner/task-get. Relevance phrases: User or agent explicitly names `planner_task_get` or equivalent (planner task get) · Workflow step that matches this capability: planner task get · Fetch one task row and its outgoing dependency ids. · Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans · Export/import task graph JSON for backup or migration · Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder. Keyword index: planner_task_get planner /mcp/planner planner-task-get planner_task_get mcp-planner-task-get tool-planner-task-get planner/planner-task-get task-get planner/task-get user or agent explicitly names `planner_task_get` or equivalent (planner task get) workflow step that matches this capability: planner task get fetch one task row and its outgoing dependency ids. task dag: dependencies, ready work, blocking relationships, multi-step implementation plans export/import task graph json for backup or migration task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder fetch one task row and its outgoing dependency ids."
alwaysApply: false
---

# `planner_task_get`

**Cursor MCP server:** `planner`  
**HTTP mount:** `/mcp/planner`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `planner-task-get`
- `planner_task_get`
- `mcp-planner-task-get`
- `tool-planner-task-get`
- `planner/planner-task-get`
- `task-get`
- `planner/task-get`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `planner_task_get` or equivalent (planner task get)
- Workflow step that matches this capability: planner task get
- Fetch one task row and its outgoing dependency ids.
- Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans
- Export/import task graph JSON for backup or migration
- Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder

### Summary

Fetch one task row and its outgoing dependency ids.

## How to use

- Required: `project`, `task_id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
