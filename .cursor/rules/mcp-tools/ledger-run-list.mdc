---
description: "MCP tool ledger_run_list (Cursor server \"ledger\", HTTP mount /mcp/ledger). Args include required project id. When: List runs for the project (open/closed filters as supported by schema). Slugs: ledger-run-list, ledger_run_list, mcp-ledger-run-list, tool-ledger-run-list, ledger/ledger-run-list, run-list, ledger/run-list. Relevance phrases: User or agent explicitly names `ledger_run_list` or equivalent (ledger run list) · Workflow step that matches this capability: ledger run list · List runs for the project (open/closed filters as supported by schema). · Episodic run log: start run, append events, search history, close run · Audit trail for cron ticks, deploys, long agent loops · Checkpoint + optional close in one ledger call. Keyword index: ledger_run_list ledger /mcp/ledger ledger-run-list ledger_run_list mcp-ledger-run-list tool-ledger-run-list ledger/ledger-run-list run-list ledger/run-list user or agent explicitly names `ledger_run_list` or equivalent (ledger run list) workflow step that matches this capability: ledger run list list runs for the project (open/closed filters as supported by schema). episodic run log: start run, append events, search history, close run audit trail for cron ticks, deploys, long agent loops checkpoint + optional close in one ledger call list runs for the project (open/closed filters as supported by schema)."
alwaysApply: false
---

# `ledger_run_list`

**Cursor MCP server:** `ledger`  
**HTTP mount:** `/mcp/ledger`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `ledger-run-list`
- `ledger_run_list`
- `mcp-ledger-run-list`
- `tool-ledger-run-list`
- `ledger/ledger-run-list`
- `run-list`
- `ledger/run-list`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `ledger_run_list` or equivalent (ledger run list)
- Workflow step that matches this capability: ledger run list
- List runs for the project (open/closed filters as supported by schema).
- Episodic run log: start run, append events, search history, close run
- Audit trail for cron ticks, deploys, long agent loops
- Checkpoint + optional close in one ledger call

### Summary

List runs for the project (open/closed filters as supported by schema).

## How to use

- Required: `project`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
