---
description: "MCP tool loop_step (Cursor server \"loop\", HTTP mount /mcp/loop). Args include required project id. When: Increment iteration counter for an open loop (end-of-tick bookkeeping). Slugs: loop-step, loop_step, mcp-loop-step, tool-loop-step, loop/loop-step. Relevance phrases: User or agent explicitly names `loop_step` or equivalent (loop step) · Workflow step that matches this capability: loop step · Increment iteration counter for an open loop (end-of-tick bookkeeping). · Agent iteration budget: open/close loop, step counter, blockers on long-running work. Keyword index: loop_step loop /mcp/loop loop-step loop_step mcp-loop-step tool-loop-step loop/loop-step user or agent explicitly names `loop_step` or equivalent (loop step) workflow step that matches this capability: loop step increment iteration counter for an open loop (end-of-tick bookkeeping). agent iteration budget: open/close loop, step counter, blockers on long-running work increment iteration counter for an open loop (end-of-tick bookkeeping)."
alwaysApply: false
---

# `loop_step`

**Cursor MCP server:** `loop`  
**HTTP mount:** `/mcp/loop`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `loop-step`
- `loop_step`
- `mcp-loop-step`
- `tool-loop-step`
- `loop/loop-step`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `loop_step` or equivalent (loop step)
- Workflow step that matches this capability: loop step
- Increment iteration counter for an open loop (end-of-tick bookkeeping).
- Agent iteration budget: open/close loop, step counter, blockers on long-running work

### Summary

Increment iteration counter for an open loop (end-of-tick bookkeeping).

## How to use

- Required: `project`, `loop_id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
