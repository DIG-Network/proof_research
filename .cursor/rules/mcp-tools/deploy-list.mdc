---
description: "MCP tool deploy_list (Cursor server \"deploy\", HTTP mount /mcp/deploy). Args include required project id. When: List deployment records for the project. Slugs: deploy-list, deploy_list, mcp-deploy-list, tool-deploy-list, deploy/deploy-list. Relevance phrases: User or agent explicitly names `deploy_list` or equivalent (deploy list) · Workflow step that matches this capability: deploy list · List deployment records for the project. · Record or search deployment history; latest per environment; update metadata. Keyword index: deploy_list deploy /mcp/deploy deploy-list deploy_list mcp-deploy-list tool-deploy-list deploy/deploy-list user or agent explicitly names `deploy_list` or equivalent (deploy list) workflow step that matches this capability: deploy list list deployment records for the project. record or search deployment history; latest per environment; update metadata list deployment records for the project."
alwaysApply: false
---

# `deploy_list`

**Cursor MCP server:** `deploy`  
**HTTP mount:** `/mcp/deploy`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `deploy-list`
- `deploy_list`
- `mcp-deploy-list`
- `tool-deploy-list`
- `deploy/deploy-list`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `deploy_list` or equivalent (deploy list)
- Workflow step that matches this capability: deploy list
- List deployment records for the project.
- Record or search deployment history; latest per environment; update metadata

### Summary

List deployment records for the project.

## How to use

- Required: `project`. Optional `environment`, `limit`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
