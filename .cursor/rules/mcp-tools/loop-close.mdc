---
description: "MCP tool loop_close (Cursor server \"loop\", HTTP mount /mcp/loop). Args include required project id. When: Close a loop when work completes or is cancelled. Slugs: loop-close, loop_close, mcp-loop-close, tool-loop-close, loop/loop-close, close, loop/close. Relevance phrases: User or agent explicitly names `loop_close` or equivalent (loop close) · Workflow step that matches this capability: loop close · Close a loop when work completes or is cancelled. · Agent iteration budget: open/close loop, step counter, blockers on long-running work. Keyword index: loop_close loop /mcp/loop loop-close loop_close mcp-loop-close tool-loop-close loop/loop-close close loop/close user or agent explicitly names `loop_close` or equivalent (loop close) workflow step that matches this capability: loop close close a loop when work completes or is cancelled. agent iteration budget: open/close loop, step counter, blockers on long-running work close a loop when work completes or is cancelled."
alwaysApply: false
---

# `loop_close`

**Cursor MCP server:** `loop`  
**HTTP mount:** `/mcp/loop`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `loop-close`
- `loop_close`
- `mcp-loop-close`
- `tool-loop-close`
- `loop/loop-close`
- `close`
- `loop/close`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `loop_close` or equivalent (loop close)
- Workflow step that matches this capability: loop close
- Close a loop when work completes or is cancelled.
- Agent iteration budget: open/close loop, step counter, blockers on long-running work

### Summary

Close a loop when work completes or is cancelled.

## How to use

- Required: `project`, `loop_id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
