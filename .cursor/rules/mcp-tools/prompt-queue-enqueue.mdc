---
description: "MCP tool prompt_queue_enqueue (Cursor server \"prompt-queue\", HTTP mount /mcp/queue). Args include required project id. When: Defer work to a later agent turn, human, or worker; FIFO per `project`. Slugs: prompt-queue-enqueue, prompt_queue_enqueue, mcp-prompt-queue-enqueue, tool-prompt-queue-enqueue, prompt-queue/prompt-queue-enqueue, enqueue, pq-enqueue, pq/enqueue. Relevance phrases: User or agent explicitly names `prompt_queue_enqueue` or equivalent (prompt queue enqueue) · Workflow step that matches this capability: prompt queue enqueue · Defer work to a later agent turn, human, or worker; FIFO per `project`. · Defer a prompt to FIFO for another process, cron tick, human, or later agent session · Serialize work across sessions with per-project queues · Background worker should pop the next item; peek without consuming. Keyword index: prompt_queue_enqueue prompt-queue /mcp/queue prompt-queue-enqueue prompt_queue_enqueue mcp-prompt-queue-enqueue tool-prompt-queue-enqueue prompt-queue/prompt-queue-enqueue enqueue pq-enqueue pq/enqueue user or agent explicitly names `prompt_queue_enqueue` or equivalent (prompt queue enqueue) workflow step that matches this capability: prompt queue enqueue defer work to a later agent turn, human, or worker; fifo per `project`. defer a prompt to fifo for another process, cron tick, human, or later agent session serialize work across sessions with per-project queues background worker should pop the next item; peek without consuming defer work to a later agent turn, human, or worker; fifo per `project`."
alwaysApply: false
---

# `prompt_queue_enqueue`

**Cursor MCP server:** `prompt-queue`  
**HTTP mount:** `/mcp/queue`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `prompt-queue-enqueue`
- `prompt_queue_enqueue`
- `mcp-prompt-queue-enqueue`
- `tool-prompt-queue-enqueue`
- `prompt-queue/prompt-queue-enqueue`
- `enqueue`
- `pq-enqueue`
- `pq/enqueue`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `prompt_queue_enqueue` or equivalent (prompt queue enqueue)
- Workflow step that matches this capability: prompt queue enqueue
- Defer work to a later agent turn, human, or worker; FIFO per `project`.
- Defer a prompt to FIFO for another process, cron tick, human, or later agent session
- Serialize work across sessions with per-project queues
- Background worker should pop the next item; peek without consuming

### Summary

Defer work to a later agent turn, human, or worker; FIFO per `project`.

## How to use

- Required: `project`, `prompt` (non-empty). Optional `metadata` JSON for routing/tags.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
