---
description: "MCP tool spec_list (Cursor server \"spec\", HTTP mount /mcp/spec). Args include required project id. When: List specs with optional status filter. Slugs: spec-list, spec_list, mcp-spec-list, tool-spec-list, spec/spec-list. Relevance phrases: User or agent explicitly names `spec_list` or equivalent (spec list) · Workflow step that matches this capability: spec list · List specs with optional status filter. · Requirements / acceptance criteria: define, link to planner tasks, set satisfaction status · Spec rollup summary for dashboards or cron hints. Keyword index: spec_list spec /mcp/spec spec-list spec_list mcp-spec-list tool-spec-list spec/spec-list user or agent explicitly names `spec_list` or equivalent (spec list) workflow step that matches this capability: spec list list specs with optional status filter. requirements / acceptance criteria: define, link to planner tasks, set satisfaction status spec rollup summary for dashboards or cron hints list specs with optional status filter."
alwaysApply: false
---

# `spec_list`

**Cursor MCP server:** `spec`  
**HTTP mount:** `/mcp/spec`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `spec-list`
- `spec_list`
- `mcp-spec-list`
- `tool-spec-list`
- `spec/spec-list`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `spec_list` or equivalent (spec list)
- Workflow step that matches this capability: spec list
- List specs with optional status filter.
- Requirements / acceptance criteria: define, link to planner tasks, set satisfaction status
- Spec rollup summary for dashboards or cron hints

### Summary

List specs with optional status filter.

## How to use

- Required: `project`. Optional `status`, `limit`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
