---
description: "MCP tool prompt_queue_peek (Cursor server \"prompt-queue\", HTTP mount /mcp/queue). Args include required project id. When: Inspect oldest queued prompt without consuming it. Slugs: prompt-queue-peek, prompt_queue_peek, mcp-prompt-queue-peek, tool-prompt-queue-peek, prompt-queue/prompt-queue-peek, pq-peek. Relevance phrases: User or agent explicitly names `prompt_queue_peek` or equivalent (prompt queue peek) · Workflow step that matches this capability: prompt queue peek · Inspect oldest queued prompt without consuming it. · Defer a prompt to FIFO for another process, cron tick, human, or later agent session · Serialize work across sessions with per-project queues · Background worker should pop the next item; peek without consuming. Keyword index: prompt_queue_peek prompt-queue /mcp/queue prompt-queue-peek prompt_queue_peek mcp-prompt-queue-peek tool-prompt-queue-peek prompt-queue/prompt-queue-peek pq-peek user or agent explicitly names `prompt_queue_peek` or equivalent (prompt queue peek) workflow step that matches this capability: prompt queue peek inspect oldest queued prompt without consuming it. defer a prompt to fifo for another process, cron tick, human, or later agent session serialize work across sessions with per-project queues background worker should pop the next item; peek without consuming inspect oldest queued prompt without consuming it."
alwaysApply: false
---

# `prompt_queue_peek`

**Cursor MCP server:** `prompt-queue`  
**HTTP mount:** `/mcp/queue`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `prompt-queue-peek`
- `prompt_queue_peek`
- `mcp-prompt-queue-peek`
- `tool-prompt-queue-peek`
- `prompt-queue/prompt-queue-peek`
- `pq-peek`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `prompt_queue_peek` or equivalent (prompt queue peek)
- Workflow step that matches this capability: prompt queue peek
- Inspect oldest queued prompt without consuming it.
- Defer a prompt to FIFO for another process, cron tick, human, or later agent session
- Serialize work across sessions with per-project queues
- Background worker should pop the next item; peek without consuming

### Summary

Inspect oldest queued prompt without consuming it.

## How to use

- Required: `project`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
