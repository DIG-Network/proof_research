---
description: "MCP tool spec_update (Cursor server \"spec\", HTTP mount /mcp/spec). Args include required project id. When: Patch spec text, status metadata, or links. Slugs: spec-update, spec_update, mcp-spec-update, tool-spec-update, spec/spec-update, update, spec/update. Relevance phrases: User or agent explicitly names `spec_update` or equivalent (spec update) · Workflow step that matches this capability: spec update · Patch spec text, status metadata, or links. · Requirements / acceptance criteria: define, link to planner tasks, set satisfaction status · Spec rollup summary for dashboards or cron hints. Keyword index: spec_update spec /mcp/spec spec-update spec_update mcp-spec-update tool-spec-update spec/spec-update update spec/update user or agent explicitly names `spec_update` or equivalent (spec update) workflow step that matches this capability: spec update patch spec text, status metadata, or links. requirements / acceptance criteria: define, link to planner tasks, set satisfaction status spec rollup summary for dashboards or cron hints patch spec text, status metadata, or links."
alwaysApply: false
---

# `spec_update`

**Cursor MCP server:** `spec`  
**HTTP mount:** `/mcp/spec`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `spec-update`
- `spec_update`
- `mcp-spec-update`
- `tool-spec-update`
- `spec/spec-update`
- `update`
- `spec/update`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `spec_update` or equivalent (spec update)
- Workflow step that matches this capability: spec update
- Patch spec text, status metadata, or links.
- Requirements / acceptance criteria: define, link to planner tasks, set satisfaction status
- Spec rollup summary for dashboards or cron hints

### Summary

Patch spec text, status metadata, or links.

## How to use

- Required: `project`, `spec_id` (or per schema).

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
