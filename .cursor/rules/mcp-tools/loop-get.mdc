---
description: "MCP tool loop_get (Cursor server \"loop\", HTTP mount /mcp/loop). Args include required project id. When: Read loop state by id. Slugs: loop-get, loop_get, mcp-loop-get, tool-loop-get, loop/loop-get. Relevance phrases: User or agent explicitly names `loop_get` or equivalent (loop get) · Workflow step that matches this capability: loop get · Read loop state by id. · Agent iteration budget: open/close loop, step counter, blockers on long-running work. Keyword index: loop_get loop /mcp/loop loop-get loop_get mcp-loop-get tool-loop-get loop/loop-get user or agent explicitly names `loop_get` or equivalent (loop get) workflow step that matches this capability: loop get read loop state by id. agent iteration budget: open/close loop, step counter, blockers on long-running work read loop state by id."
alwaysApply: false
---

# `loop_get`

**Cursor MCP server:** `loop`  
**HTTP mount:** `/mcp/loop`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `loop-get`
- `loop_get`
- `mcp-loop-get`
- `tool-loop-get`
- `loop/loop-get`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `loop_get` or equivalent (loop get)
- Workflow step that matches this capability: loop get
- Read loop state by id.
- Agent iteration budget: open/close loop, step counter, blockers on long-running work

### Summary

Read loop state by id.

## How to use

- Required: `project`, `loop_id`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
