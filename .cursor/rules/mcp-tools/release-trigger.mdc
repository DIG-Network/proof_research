---
description: "MCP tool release_trigger (Cursor server \"release\", HTTP mount /mcp/release). Args include required project id. When: Trigger release automation (webhook / EB) when host policy allows. Slugs: release-trigger, release_trigger, mcp-release-trigger, tool-release-trigger, release/release-trigger, trigger, release/trigger. Relevance phrases: User or agent explicitly names `release_trigger` or equivalent (release trigger) · Workflow step that matches this capability: release trigger · Trigger release automation (webhook / EB) when host policy allows. · Release automation jobs: trigger, list, get status (host may disable or allowlist webhooks). Keyword index: release_trigger release /mcp/release release-trigger release_trigger mcp-release-trigger tool-release-trigger release/release-trigger trigger release/trigger user or agent explicitly names `release_trigger` or equivalent (release trigger) workflow step that matches this capability: release trigger trigger release automation (webhook / eb) when host policy allows. release automation jobs: trigger, list, get status (host may disable or allowlist webhooks) trigger release automation (webhook / eb) when host policy allows."
alwaysApply: false
---

# `release_trigger`

**Cursor MCP server:** `release`  
**HTTP mount:** `/mcp/release`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `release-trigger`
- `release_trigger`
- `mcp-release-trigger`
- `tool-release-trigger`
- `release/release-trigger`
- `trigger`
- `release/trigger`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `release_trigger` or equivalent (release trigger)
- Workflow step that matches this capability: release trigger
- Trigger release automation (webhook / EB) when host policy allows.
- Release automation jobs: trigger, list, get status (host may disable or allowlist webhooks)

### Summary

Trigger release automation (webhook / EB) when host policy allows.

## How to use

- May be disabled or allowlist-gated; stop if tool errors as disabled.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
