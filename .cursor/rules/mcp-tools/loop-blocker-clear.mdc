---
description: "MCP tool loop_blocker_clear (Cursor server \"loop\", HTTP mount /mcp/loop). Args include required project id. When: Clear blockers when resolved. Slugs: loop-blocker-clear, loop_blocker_clear, mcp-loop-blocker-clear, tool-loop-blocker-clear, loop/loop-blocker-clear, blocker-clear, loop/blocker-clear. Relevance phrases: User or agent explicitly names `loop_blocker_clear` or equivalent (loop blocker clear) · Workflow step that matches this capability: loop blocker clear · Clear blockers when resolved. · Agent iteration budget: open/close loop, step counter, blockers on long-running work. Keyword index: loop_blocker_clear loop /mcp/loop loop-blocker-clear loop_blocker_clear mcp-loop-blocker-clear tool-loop-blocker-clear loop/loop-blocker-clear blocker-clear loop/blocker-clear user or agent explicitly names `loop_blocker_clear` or equivalent (loop blocker clear) workflow step that matches this capability: loop blocker clear clear blockers when resolved. agent iteration budget: open/close loop, step counter, blockers on long-running work clear blockers when resolved."
alwaysApply: false
---

# `loop_blocker_clear`

**Cursor MCP server:** `loop`  
**HTTP mount:** `/mcp/loop`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `loop-blocker-clear`
- `loop_blocker_clear`
- `mcp-loop-blocker-clear`
- `tool-loop-blocker-clear`
- `loop/loop-blocker-clear`
- `blocker-clear`
- `loop/blocker-clear`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `loop_blocker_clear` or equivalent (loop blocker clear)
- Workflow step that matches this capability: loop blocker clear
- Clear blockers when resolved.
- Agent iteration budget: open/close loop, step counter, blockers on long-running work

### Summary

Clear blockers when resolved.

## How to use

- Required fields per schema.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
