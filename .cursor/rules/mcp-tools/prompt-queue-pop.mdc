---
description: "MCP tool prompt_queue_pop (Cursor server \"prompt-queue\", HTTP mount /mcp/queue). Args include required project id. When: You are the consumer for that queue and should remove the oldest item. Slugs: prompt-queue-pop, prompt_queue_pop, mcp-prompt-queue-pop, tool-prompt-queue-pop, prompt-queue/prompt-queue-pop, pq-pop. Relevance phrases: User or agent explicitly names `prompt_queue_pop` or equivalent (prompt queue pop) · Workflow step that matches this capability: prompt queue pop · You are the consumer for that queue and should remove the oldest item. · Defer a prompt to FIFO for another process, cron tick, human, or later agent session · Serialize work across sessions with per-project queues · Background worker should pop the next item; peek without consuming. Keyword index: prompt_queue_pop prompt-queue /mcp/queue prompt-queue-pop prompt_queue_pop mcp-prompt-queue-pop tool-prompt-queue-pop prompt-queue/prompt-queue-pop pq-pop user or agent explicitly names `prompt_queue_pop` or equivalent (prompt queue pop) workflow step that matches this capability: prompt queue pop you are the consumer for that queue and should remove the oldest item. defer a prompt to fifo for another process, cron tick, human, or later agent session serialize work across sessions with per-project queues background worker should pop the next item; peek without consuming you are the consumer for that queue and should remove the oldest item."
alwaysApply: false
---

# `prompt_queue_pop`

**Cursor MCP server:** `prompt-queue`  
**HTTP mount:** `/mcp/queue`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `prompt-queue-pop`
- `prompt_queue_pop`
- `mcp-prompt-queue-pop`
- `tool-prompt-queue-pop`
- `prompt-queue/prompt-queue-pop`
- `pq-pop`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `prompt_queue_pop` or equivalent (prompt queue pop)
- Workflow step that matches this capability: prompt queue pop
- You are the consumer for that queue and should remove the oldest item.
- Defer a prompt to FIFO for another process, cron tick, human, or later agent session
- Serialize work across sessions with per-project queues
- Background worker should pop the next item; peek without consuming

### Summary

You are the consumer for that queue and should remove the oldest item.

## How to use

- Required: `project`. Empty queue returns depth 0 / empty marker.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
