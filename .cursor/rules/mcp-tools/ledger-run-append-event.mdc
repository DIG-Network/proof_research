---
description: "MCP tool ledger_run_append_event (Cursor server \"ledger\", HTTP mount /mcp/ledger). Args include required project id. When: Log a milestone inside an open run. Slugs: ledger-run-append-event, ledger_run_append_event, mcp-ledger-run-append-event, tool-ledger-run-append-event, ledger/ledger-run-append-event, run-append-event, ledger/run-append-event. Relevance phrases: User or agent explicitly names `ledger_run_append_event` or equivalent (ledger run append event) · Workflow step that matches this capability: ledger run append event · Log a milestone inside an open run. · Episodic run log: start run, append events, search history, close run · Audit trail for cron ticks, deploys, long agent loops · Checkpoint + optional close in one ledger call. Keyword index: ledger_run_append_event ledger /mcp/ledger ledger-run-append-event ledger_run_append_event mcp-ledger-run-append-event tool-ledger-run-append-event ledger/ledger-run-append-event run-append-event ledger/run-append-event user or agent explicitly names `ledger_run_append_event` or equivalent (ledger run append event) workflow step that matches this capability: ledger run append event log a milestone inside an open run. episodic run log: start run, append events, search history, close run audit trail for cron ticks, deploys, long agent loops checkpoint + optional close in one ledger call log a milestone inside an open run."
alwaysApply: false
---

# `ledger_run_append_event`

**Cursor MCP server:** `ledger`  
**HTTP mount:** `/mcp/ledger`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `ledger-run-append-event`
- `ledger_run_append_event`
- `mcp-ledger-run-append-event`
- `tool-ledger-run-append-event`
- `ledger/ledger-run-append-event`
- `run-append-event`
- `ledger/run-append-event`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `ledger_run_append_event` or equivalent (ledger run append event)
- Workflow step that matches this capability: ledger run append event
- Log a milestone inside an open run.
- Episodic run log: start run, append events, search history, close run
- Audit trail for cron ticks, deploys, long agent loops
- Checkpoint + optional close in one ledger call

### Summary

Log a milestone inside an open run.

## How to use

- Required: `project`, `run_id`, `summary`. Optional `kind`, `detail`, `refs`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
