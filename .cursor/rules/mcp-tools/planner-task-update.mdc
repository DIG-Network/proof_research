---
description: "MCP tool planner_task_update (Cursor server \"planner\", HTTP mount /mcp/planner). Args include required project id. When: Patch task fields after progress or scope change. Slugs: planner-task-update, planner_task_update, mcp-planner-task-update, tool-planner-task-update, planner/planner-task-update, task-update, planner/task-update. Relevance phrases: User or agent explicitly names `planner_task_update` or equivalent (planner task update) · Workflow step that matches this capability: planner task update · Patch task fields after progress or scope change. · Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans · Export/import task graph JSON for backup or migration · Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder. Keyword index: planner_task_update planner /mcp/planner planner-task-update planner_task_update mcp-planner-task-update tool-planner-task-update planner/planner-task-update task-update planner/task-update user or agent explicitly names `planner_task_update` or equivalent (planner task update) workflow step that matches this capability: planner task update patch task fields after progress or scope change. task dag: dependencies, ready work, blocking relationships, multi-step implementation plans export/import task graph json for backup or migration task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder patch task fields after progress or scope change."
alwaysApply: false
---

# `planner_task_update`

**Cursor MCP server:** `planner`  
**HTTP mount:** `/mcp/planner`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `planner-task-update`
- `planner_task_update`
- `mcp-planner-task-update`
- `tool-planner-task-update`
- `planner/planner-task-update`
- `task-update`
- `planner/task-update`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `planner_task_update` or equivalent (planner task update)
- Workflow step that matches this capability: planner task update
- Patch task fields after progress or scope change.
- Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans
- Export/import task graph JSON for backup or migration
- Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder

### Summary

Patch task fields after progress or scope change.

## How to use

- Required: `project`, `task_id`. Optional `title`, `description` (body), `status`, `parent_task_id`, `metadata`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
