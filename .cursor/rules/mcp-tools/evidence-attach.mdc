---
description: "MCP tool evidence_attach (Cursor server \"evidence\", HTTP mount /mcp/evidence). Args include required project id. When: Attach proof (logs, outputs) to a spec/deploy/verify context. Slugs: evidence-attach, evidence_attach, mcp-evidence-attach, tool-evidence-attach, evidence/evidence-attach, attach, evidence/attach. Relevance phrases: User or agent explicitly names `evidence_attach` or equivalent (evidence attach) · Workflow step that matches this capability: evidence attach · Attach proof (logs, outputs) to a spec/deploy/verify context. · Attach proof artifacts to specs/deploys/verify runs; export evidence bundles for handoff. Keyword index: evidence_attach evidence /mcp/evidence evidence-attach evidence_attach mcp-evidence-attach tool-evidence-attach evidence/evidence-attach attach evidence/attach user or agent explicitly names `evidence_attach` or equivalent (evidence attach) workflow step that matches this capability: evidence attach attach proof (logs, outputs) to a spec/deploy/verify context. attach proof artifacts to specs/deploys/verify runs; export evidence bundles for handoff attach proof (logs, outputs) to a spec/deploy/verify context."
alwaysApply: false
---

# `evidence_attach`

**Cursor MCP server:** `evidence`  
**HTTP mount:** `/mcp/evidence`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `evidence-attach`
- `evidence_attach`
- `mcp-evidence-attach`
- `tool-evidence-attach`
- `evidence/evidence-attach`
- `attach`
- `evidence/attach`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `evidence_attach` or equivalent (evidence attach)
- Workflow step that matches this capability: evidence attach
- Attach proof (logs, outputs) to a spec/deploy/verify context.
- Attach proof artifacts to specs/deploys/verify runs; export evidence bundles for handoff

### Summary

Attach proof (logs, outputs) to a spec/deploy/verify context.

## How to use

- Required fields per schema (project + attachment payload).

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
