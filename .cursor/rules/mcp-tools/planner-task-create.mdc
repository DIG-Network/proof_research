---
description: "MCP tool planner_task_create (Cursor server \"planner\", HTTP mount /mcp/planner). Args include required project id. When: Add a node to the task DAG (title required). Slugs: planner-task-create, planner_task_create, mcp-planner-task-create, tool-planner-task-create, planner/planner-task-create, task-create, planner/task-create. Relevance phrases: User or agent explicitly names `planner_task_create` or equivalent (planner task create) · Workflow step that matches this capability: planner task create · Add a node to the task DAG (title required). · Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans · Export/import task graph JSON for backup or migration · Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder. Keyword index: planner_task_create planner /mcp/planner planner-task-create planner_task_create mcp-planner-task-create tool-planner-task-create planner/planner-task-create task-create planner/task-create user or agent explicitly names `planner_task_create` or equivalent (planner task create) workflow step that matches this capability: planner task create add a node to the task dag (title required). task dag: dependencies, ready work, blocking relationships, multi-step implementation plans export/import task graph json for backup or migration task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder add a node to the task dag (title required)."
alwaysApply: false
---

# `planner_task_create`

**Cursor MCP server:** `planner`  
**HTTP mount:** `/mcp/planner`

## Cursor routing (read this)

- There is **no separate frontmatter field** for substring or keyword matching. These rules use **`description`** (for Apply intelligently) and the body below.
- For **Apply intelligently**, Cursor presents **`description`** to the Agent so it can decide relevance (tool name, server, mount, slugs, and user-intent phrases).

## Orchestration

- **Multi-step flows, mount matrix, ordering, anti-patterns:** `hosted-mcp-orchestration.mdc` (always apply).
- **Exhaustive tool list:** `mcp-tools-when-to-use-always.mdc`.

## When to use

### Slugs

Use this tool when labels, tags, routing keys, or internal references match any of:

- `planner-task-create`
- `planner_task_create`
- `mcp-planner-task-create`
- `tool-planner-task-create`
- `planner/planner-task-create`
- `task-create`
- `planner/task-create`

### Triggers

Prefer this tool when any of the following apply:

- User or agent explicitly names `planner_task_create` or equivalent (planner task create)
- Workflow step that matches this capability: planner task create
- Add a node to the task DAG (title required).
- Task DAG: dependencies, ready work, blocking relationships, multi-step implementation plans
- Export/import task graph JSON for backup or migration
- Task backlogs, roadmaps, agent task files, or issue breakdowns in any editor or folder

### Summary

Add a node to the task DAG (title required).

## How to use

- Required: `project`, `title`. Optional `description`, `status`, `parent_task_id`, `task_id`, `metadata`.

## Project

Pass the same stable `project` string on every tool call for this mount (alphanumeric start; then letters, digits, `.`, `_`, `-`).
